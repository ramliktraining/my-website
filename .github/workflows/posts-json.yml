name: Generate losts.json

on:
  push:
    paths:
      - 'losts/**.md'
  workflow_dispatch:

  jobs:
  generate-losts-json:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install gray-matter
        run: npm install gray-matter

      - name: Generate losts.json
        run: |
          node -e "
            const fs = require('fs');
            const matter = require('gray-matter');
            const losts = fs.readdirSync('losts')
              .filter(f => f.endsWith('.md'))
              .map(f => {
                const content = fs.readFileSync('losts/' + f, 'utf8');
                const { data } = matter(content);
                return {
                  title: data.title,
                  date: data.date,
                  slug: data.slug || f.replace(/\.md$/, ''),
                  cover: data.cover || '',
                  description: data.description || ''
                };
              });
            fs.writeFileSync('losts.json', JSON.stringify(losts, null, 2));
          "

      - name: Commit and Push
        env:
          PERSONAL_TOKEN: ${{ secrets.PERSONAL_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote set-url origin https://x-access-token:${PERSONAL_TOKEN}@github.com/${{ github.repository }}
          git add losts.json
          git commit -m "🤖 Auto-generate losts.json" || echo "No changes"
          git push origin HEAD:main
